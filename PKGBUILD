# Maintainer: Electria
pkgname=waydroid-libndk
pkgver=1
pkgrel=1
pkgdesc="libndk arm translation for Waydroid"
arch=(x86_64)
url=
license=('GPL-3.0-or-later')
depends=()
makedepends=('git')

source=()
sha256sums=('SKIP')

pkgver() {
  cd "$pkgname"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short=7 HEAD)"
}

package() {

}
